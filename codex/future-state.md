# Future State

## Remaining Future Improvements Only

- Replace the past configured workshop date with a real future date when available.
- Confirm and update price/capacity if the business values changed.
- Add real approved workshop photography when available, following `assets/README.md`.
- Decide whether `?edit=1` should remain in production or be retired in favor of a safer content workflow.
- If the page stays live long-term, consider reducing date duplication between HTML fallback text and JS config.
- If future maintenance grows, consider adding lightweight automated smoke checks for static asset integrity and key state rendering.
